CREATE TABLE sales_hash (
    id BIGINT NOT NULL AUTO_INCREMENT,
    Region VARCHAR(50),
    Country VARCHAR(50),
    Item_Type VARCHAR(50),
    Sales_Channel VARCHAR(20),
    Order_Priority CHAR(1),
    Order_Date DATE,
    Order_ID BIGINT,
    Ship_Date DATE,
    Units_Sold INT,
    Unit_Price DECIMAL(10,2),
    Unit_Cost DECIMAL(10,2),
    Total_Revenue DECIMAL(15,2),
    Total_Cost DECIMAL(15,2),
    Total_Profit DECIMAL(15,2),
    PRIMARY KEY (ID, Order_ID)
)

PARTITION BY HASH (Order_ID)
PARTITIONS 6;

LOAD DATA INFILE 'C:\Users\NUHAA\Downloads\sales_2021_2024.csv''
INTO TABLE sales_source
FIELDS TERMINATED BY ','
ENCLOSED BY '"'
LINES TERMINATED BY '\n'
IGNORE 1 ROWS
(Region, Country, Item_Type, Sales_Channel, Order_Priority,
 @Order_Date, Order_ID, @Ship_Date, Units_Sold,
 Unit_Price, Unit_Cost, Total_Revenue, Total_Cost, Total_Profit)
SET
 Order_Date = STR_TO_DATE(@Order_Date, '%m/%d/%Y'),
 Ship_Date  = STR_TO_DATE(@Ship_Date, '%m/%d/%Y');




INSERT INTO sales_hash
SELECT * FROM sales_source;

SELECT
    PARTITION_NAME,
    TABLE_ROWS
FROM INFORMATION_SCHEMA.PARTITIONS
WHERE TABLE_NAME = 'sales_hash';

EXPLAIN ANALYZE
SELECT *
FROM sales_hash
WHERE Order_ID = 7734891;

EXPLAIN ANALYZE
SELECT *
FROM sales_source
WHERE Order_ID = 7734891;
