-- Query Execution Time
-- Partitioned
-- Initialize total time
SET @total_time = 0;

-- Run 1
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 2
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 3
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 4
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 5
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 6
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 7
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 8
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 9
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 10
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_hash
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Calculate average
SELECT @total_time / 10 AS avg_time_seconds;

-- Non-partitioned
-- Initialize total time
SET @total_time = 0;

-- Run 1
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 2
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 3
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 4
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 5
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 6
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 7
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 8
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 9
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 10
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM sales_database.sales
WHERE Item_Type = 'Snacks'
  AND Region = 'Europe'
  AND Total_Revenue > 100000;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Calculate average
SELECT @total_time / 10 AS avg_time_seconds;


-- Throughput
-- Partitioned
DELIMITER $$

CREATE PROCEDURE test_hash_partitioned(IN runs INT)
BEGIN
    DECLARE i INT DEFAULT 0;
    DECLARE start_time DOUBLE;
    DECLARE end_time DOUBLE;
    DECLARE total_time DOUBLE;
    DECLARE dummy INT;

    -- Start time with microseconds
    SET start_time = UNIX_TIMESTAMP(NOW(6));

    -- Loop to run the query multiple times
    WHILE i < runs DO
        -- Use INTO @dummy to avoid returning rows
        SELECT COUNT(*) INTO dummy 
        FROM sales
		WHERE Region = 'Europe'
		AND Item_Type = 'Snacks'
		AND Total_Revenue > 100000;
        SET i = i + 1;
    END WHILE;

    -- End time with microseconds
    SET end_time = UNIX_TIMESTAMP(NOW(6));

    -- Total elapsed time in seconds (decimal)
    SET total_time = end_time - start_time;

    -- Show only QPS result
    SELECT runs / total_time AS queries_per_second;
END$$

DELIMITER ;

-- Run for 10 queries
CALL test_hash_partitioned(10);

-- Non-partitioned
DELIMITER $$

CREATE PROCEDURE test_nonpartitioned1(IN runs INT)
BEGIN
    DECLARE i INT DEFAULT 0;
    DECLARE start_time DOUBLE;
    DECLARE end_time DOUBLE;
    DECLARE total_time DOUBLE;
    DECLARE dummy INT;

    -- Start time with microseconds
    SET start_time = UNIX_TIMESTAMP(NOW(6));

    -- Loop to run the query multiple times
    WHILE i < runs DO
        -- Use INTO @dummy to avoid returning rows
        SELECT COUNT(*) INTO dummy 
        FROM sales
		WHERE Region = 'Europe'
		AND Item_Type = 'Snacks'
		AND Total_Revenue > 100000;
        SET i = i + 1;
    END WHILE;

    -- End time with microseconds
    SET end_time = UNIX_TIMESTAMP(NOW(6));

    -- Total elapsed time in seconds (decimal)
    SET total_time = end_time - start_time;

    -- Show only QPS result
    SELECT runs / total_time AS queries_per_second;
END$$

DELIMITER ;

-- Run for 10 queries
CALL test_nonpartitioned1(10);

-- Scalability
-- Partitioned
-- Initialize total times
SET @total_time_small = 0;
SET @total_time_medium = 0;
SET @total_time_large = 0;

-- Small dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales_hash
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-01-07';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_small = @end - @start;

-- Medium dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales_hash
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-03-31';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_medium = @end - @start;

-- Large dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales_hash
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-12-31';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_large = @end - @start;

-- Show results
SELECT 
    @total_time_small AS small_dataset_seconds,
    @total_time_medium AS medium_dataset_seconds,
    @total_time_large AS large_dataset_seconds;

-- Non-partitioned
-- Initialize total times
SET @total_time_small = 0;
SET @total_time_medium = 0;
SET @total_time_large = 0;

-- Small dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-01-07';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_small = @end - @start;

-- Medium dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-03-31';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_medium = @end - @start;

-- Large dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM sales
WHERE Region = 'Europe'
  AND Item_Type = 'Snacks'
  AND Order_Date BETWEEN '2023-01-01' AND '2023-12-31';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_large = @end - @start;

-- Show results
SELECT 
    @total_time_small AS small_dataset_seconds,
    @total_time_medium AS medium_dataset_seconds,
    @total_time_large AS large_dataset_seconds;
