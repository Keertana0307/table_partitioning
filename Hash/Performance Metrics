-- RUN 1
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 2
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 3
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 4

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 5

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 6
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 7
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 8


SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 9

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 10
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_hash_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

Range Partitioned

-- RUN 1
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 2
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 3
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM  sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 4

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 5

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 6
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 7
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 8


SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 9

SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);

-- RUN 10
SET @start = UNIX_TIMESTAMP(NOW(6));

SELECT * 
FROM sales_range_partitioned
WHERE customer_id = 1001
  AND region = 'Europe';

SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_access_time = @total_access_time + (@end - @start);


Calculate Average:
SELECT 
  (@total_access_time / @runs) AS avg_access_time_seconds;


Execution Time
-- Non-partitioned
-- Initialize total time
SET @total_time = 0;
 
-- Run 1
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 2
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 3
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 4
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 5
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 6
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 7
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 8
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 9
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Run 10
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT * FROM hash_range_sales
WHERE country='Canada' AND order_date='2022-01-01';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);
 
-- Calculate average
SELECT @total_time / 10 AS avg_query_execution_time_seconds;

Scalability
-- Non-partitioned
-- Initialize total times
SET @total_time_small = 0;
SET @total_time_medium = 0;
SET @total_time_large = 0;
 
-- Small dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM database_sales.sales
WHERE Country = 'Canada'
    AND Ship_Date BETWEEN '2022-01-01' AND '2022-01-07';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_small = @end - @start;
 
-- Medium dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM database_sales.sales
WHERE Country = 'Canada'
    AND Ship_Date BETWEEN '2022-01-01' AND '2022-01-07';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_medium = @end - @start;
 
-- Large dataset
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT COUNT(*) 
FROM database_sales.sales
WHERE Country = 'Canada'
    AND Ship_Date BETWEEN '2022-01-01' AND '2022-01-07';
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time_large = @end - @start;
 
-- Show results
SELECT 
    @total_time_small AS small_dataset_seconds,
    @total_time_medium AS medium_dataset_seconds,
    @total_time_large AS large_dataset_seconds;

