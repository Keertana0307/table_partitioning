-- Query Execution Time
-- Partitioned
-- Initialize total time
SET @total_time = 0;

-- Run 1
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 2
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 3
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 4
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 5
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 6
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 7
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 8
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 9
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 10
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales_range_hash_partitioned
where region_id = 1
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Calculate average
SELECT @total_time / 10 AS avg_query_execution_time_seconds;

-- Non-partitioned
-- Initialize total time
SET @total_time = 0;

-- Run 1
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 2
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 3
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 4
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 5
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 6
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 7
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 8
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 9
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Run 10
SET @start = UNIX_TIMESTAMP(NOW(6));
SELECT YEAR(Order_Date) AS order_year, Region, SUM(Total_Revenue) AS total_revenue
FROM sales
where Region = 'Europe'
GROUP BY order_year, Region;
SET @end = UNIX_TIMESTAMP(NOW(6));
SET @total_time = @total_time + (@end - @start);

-- Calculate average
SELECT @total_time / 10 AS avg_query_execution_time_seconds;

-- Throughput
-- Partitioned
DELIMITER $$

CREATE PROCEDURE test_rangehash_partitioned(IN runs INT)
BEGIN
    DECLARE i INT DEFAULT 0;
    DECLARE start_time DOUBLE;
    DECLARE end_time DOUBLE;
    DECLARE total_time DOUBLE;
    DECLARE dummy INT;

    -- Start time with microseconds
    SET start_time = UNIX_TIMESTAMP(NOW(6));

    -- Loop to run the query multiple times
    WHILE i < runs DO
        -- Use INTO @dummy to avoid returning rows
        SELECT COUNT(*) INTO dummy 
		FROM sales_range_hash_partitioned
		where region_id = 1
        AND year(Order_Date) = '2023';
        SET i = i + 1;
    END WHILE;

    -- End time with microseconds
    SET end_time = UNIX_TIMESTAMP(NOW(6));

    -- Total elapsed time in seconds (decimal)
    SET total_time = end_time - start_time;

    -- Show only QPS result
    SELECT runs / total_time AS queries_per_second;
END$$

DELIMITER ;

-- Run for 10 queries
CALL test_rangehash_partitioned(10);


-- Non-partitioned
DELIMITER $$

CREATE PROCEDURE test_nonpartitioned4(IN runs INT)
BEGIN
    DECLARE i INT DEFAULT 0;
    DECLARE start_time DOUBLE;
    DECLARE end_time DOUBLE;
    DECLARE total_time DOUBLE;
    DECLARE dummy INT;

    -- Start time with microseconds
    SET start_time = UNIX_TIMESTAMP(NOW(6));

    -- Loop to run the query multiple times
    WHILE i < runs DO
        -- Use INTO @dummy to avoid returning rows
        SELECT COUNT(*) INTO dummy 
		FROM sales
		where Region = 'Europe'
        AND year(Order_Date) = '2023';
        SET i = i + 1;
    END WHILE;

    -- End time with microseconds
    SET end_time = UNIX_TIMESTAMP(NOW(6));

    -- Total elapsed time in seconds (decimal)
    SET total_time = end_time - start_time;

    -- Show only QPS result
    SELECT runs / total_time AS queries_per_second;
END$$

DELIMITER ;

-- Run for 10 queries
CALL test_nonpartitioned4(10);

